# Config

- **`requirements.txt`** — Python dependencies for `scripts/` (standard library only for current generators).

Install from repository root:

```powershell
pip install -r config/requirements.txt
```
