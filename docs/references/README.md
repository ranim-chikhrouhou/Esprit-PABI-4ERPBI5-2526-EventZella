# References

Optional: drop PDF references here for version control (e.g. `Liste Des Kpis.pdf`, CDC extracts).

The DAX build script looks for `Liste Des Kpis.pdf` in this folder first, then in the repository root.
