# Database

SQL Server **`.bak`** backups of the **Staging Area (SA)** and **Data Warehouse (DW)**.

> Watch file size: prefer **Git LFS**, institutional storage, or a **Release** instead of bloating standard Git history.
